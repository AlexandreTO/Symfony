<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        {% block stylesheets %}
            <link rel="stylesheet" href="{{asset('style.css')}}">
        {% endblock %}
        <meta charset="UTF-8">
        <title>Welcome !</title>
    </head>
        <!-- Fonts -->
    <body>
        {% block header %}
        <div class="header">
             <ul>
                <li><a href="{{path('homepage')}}" >Form</a></li>
                <li><a href="{{path('admin_book')}}" >Book List</a></li>
                <li style="float:right">
                {% if app.user %}
                    <a href="{{ path('app_logout') }}">Logout</a>
                {% endif %}
            </li>
            <li style="float:right">
                <a>
                    {% if app.user %}
                    Bonjour  {{ app.user.username }} !
                    {% endif %}
                </a>
            </li>
            </ul> 
        </div> 
        {% endblock %}
        <br/>
        {% block body %}
        <div id="body_content">
            {% block content %}{% endblock %}
        </div>
        {% endblock %}
    </body>
</html>